<style type="text/css">
    body {
        background: #1d4666;
    }

    a {
        color: #fff;
    }

    .wrap {
        margin: 0;
        padding: 0;
    }

    .top {
        color: #fff;
        height: 4rem;
        line-height: 4rem;
        padding: 0.1rem;
        background: #172f49;
        border: 0;
    }
        .top .hotelselect {
            max-width: 300px;
        }

        .top .posposselect {
            max-width: 300px;
        }

        .top > span {
            float: left;
            font-size: 1.5rem;
            margin-right: 1.5rem;
        }

            .top > span:first-child > img {
                margin: 0.8rem;
            }

            .top > span:last-child {
                float: right;
            }

                .top > span:last-child img {
                    margin: 0.5rem;
                }

    .content {
        clear: both;
        background: #1d4666;
    }

        .content li {
            width: 25%;
            color: #ddd;
            float: left;
            text-align: center;
            margin: 1rem 0 1rem 0;
        }

            .content li span {
                padding-top: 0.5rem;
                font-size: 2rem;
            }

    .apply-info {
        background: #1d4666;
    }

        .apply-info h3 {
            border: 0;
        }

    .wrapper {
        margin-top: 0;
        background: #1d4666;
    }

    .k-dropdown-wrap {
        color: #fff;
        background: #1d4666;
    }

    .k-state-default {
        color: #fff;
        background: #1d4666;
    }

    .k-input, k-select {
        color: #fff;
    }

    .footer {
        bottom: 0;
        margin: 0;
        padding: 0;
        color: #ddd;
        width: 100%;
        position: absolute;
        background: #1d4666;
    }


    @@media screen and (max-width: 1280px) {


        .top .hotelselect {
            max-width:14rem;       
        }
            .top .posposselect {
                max-width: 14rem;
            }
            .top > span {
                float: left;
                font-size:1.2rem; 
                margin-right: 1.2rem;
            }
        .top .k-dropdown-wrap.k-state-default {
            padding-right: 0.5rem;
        }
                .top > span:first-child > img {
                    margin: 0.8rem;
                }

                .top > span:last-child {
                    float: right;
                }

                    .top > span:last-child img {
                        margin: 0.5rem;
                    }
    }


    @@media screen and (max-width: 1090    px) {


            .top .hotelselect {
                max-width: 11rem;
            }

            .top .posposselect {
                max-width: 11rem;
            }

            .top > span {
                float: left;
                font-size: 1rem;
                margin-right: 1rem;
            }
        .top .k-dropdown-wrap.k-state-default {
          padding-right:0.5rem;
        }

        .top > span:first-child > img {
            margin: 0.8rem;
        }

                .top > span:last-child {
                    float: right;
                }

                    .top > span:last-child img {
                        margin: 0.5rem;
                    }
    }






    /* layer 自定义样式 */
    .layui-layer-btn {
        margin-top: 50px;
    }

        .layui-layer-btn .layui-layer-btn0 {
            border-color: #1E9FFF;
            background-color: #f55858;
            width: 50%;
            color: #fff;
            border: 0;
            width: 45%;
            padding: 0.5rem;
            font-size: 1.5rem;
            border-top: 1px solid #ccc;
            left: 0;
            bottom: 0;
            clear: both;
            text-align: center;
            position: absolute;
            border-bottom-left-radius: 1rem;
        }

        .layui-layer-btn a {
            margin: 0 0 0 0;
        }

        .layui-layer-btn .layui-layer-btn1 {
            border: 0;
            width: 45%;
            padding: 0.5rem;
            font-size: 1.5rem;
            border-top: 1px solid #ccc;
            left: 50%;
            bottom: 0;
            clear: both;
            text-align: center;
            position: absolute;
            border-bottom-right-radius: 1rem;
        }

    .layui-layer-title {
        font-size: 2rem;
        color: #fff;
        overflow: hidden;
        text-align: center;
        background-color: #8a8a8a;
        border-radius: 1rem 1rem 0 0;
    }

    .layui-layer {
        border-radius: 1rem;
        border: 1px solid #777;
    }
</style>