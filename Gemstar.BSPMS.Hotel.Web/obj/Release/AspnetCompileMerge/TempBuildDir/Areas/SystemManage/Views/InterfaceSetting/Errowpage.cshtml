@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
    <meta name="viewport" content="width=device-width" />
    <title>提示</title>
</head>
<body>
    <hgroup>
        <h1>提示</h1>
        <h2 id="h2" style="margin-left:260px;font-size:29px;">本地接口程序没有启动,</h2>
    </hgroup>
</body>
</html>
<script type="text/javascript">
    window.onload = ajax('@Url.Action("DownloadFile")');
        function ajax(url) {
            var xmlHttpReg = null;
            if (window.ActiveXObject) {//如果是IE

                xmlHttpReg = new ActiveXObject("Microsoft.XMLHTTP");
            } else if (window.XMLHttpRequest) {
                xmlHttpReg = new XMLHttpRequest(); //实例化一个xmlHttpReg
            }
            //如果实例化成功,就调用open()方法,就开始准备向服务器发送请求
            if (xmlHttpReg != null) {
                xmlHttpReg.open("post", url, true);1
                xmlHttpReg.send(null);
                xmlHttpReg.onreadystatechange = doResult; //设置回调函数
            }
            //一旦readyState的值改变,将会调用这个函数,readyState=4表示完成相应
            function doResult() {
                if (xmlHttpReg.readyState == 4) {//4代表执行完成
                    if (xmlHttpReg.status == 200) {//200代表执行成功
                        if (xmlHttpReg.responseText != null || typeof (xmlHttpReg.responseText) != "undefined") {
                            var hyperlink = '<a href=' + xmlHttpReg.responseText + '  target="_blank">点击我下载最新的升级包</a>'
                            document.getElementById("h2").innerHTML += hyperlink;
                        }
                    }
                }
            
            }
             
       
        }
</script>
  